# Required: expo-router's transitive web deps (react-dom) declare strict
# peer ranges that conflict with Expo's pinned react version under npm's
# strict resolver. legacy-peer-deps restores npm v6 behavior for peers only.
legacy-peer-deps=true
