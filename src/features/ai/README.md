# src/features/ai

Gemini AI insights (future).

Not implemented yet. When built:
- API keys stay server-side (e.g. Supabase Edge Functions) — never in the app.
- Generated insights persist to the `ai_insights` table under RLS.
